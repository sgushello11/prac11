<Window x:Class="DocumentFlowApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" FontSize="14" Margin="5"/>
        <TextBox x:Name="txtLogin" Grid.Row="0" Grid.Column="1" Margin="5" FontSize="14"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" FontSize="14" Margin="5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5" FontSize="14"/>

        <Button x:Name="btnLogin" Grid.Row="2" Grid.Column="1" Content="Войти" 
                Width="100" Height="30" Margin="5" Click="btnLogin_Click" 
                Background="Green" Foreground="White" FontSize="14"/>

        <TextBlock x:Name="tbError" Grid.Row="3" Grid.ColumnSpan="2" 
                   Foreground="Red" TextAlignment="Center" Margin="5"/>
    </Grid>
</Window>